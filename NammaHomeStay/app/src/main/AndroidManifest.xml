<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NammaHomeStay">

        <!-- Splash Screen — opens first -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.NammaHomeStay.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Screen -->
        <activity
            android:name="auth.LoginActivity"
            android:exported="false" />

        <!-- Main Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Host Screens -->
        <activity
            android:name="host.HomeProfileActivity"
            android:exported="false" />

        <activity
            android:name="host.DailyMenuActivity"
            android:exported="false" />

        <activity
            android:name="host.InquiryBoxActivity"
            android:exported="false" />

        <activity
            android:name="host.LocalGuideActivity"
            android:exported="false" />

        <activity
            android:name=".HomeStayListActivity"
            android:exported="false" />

    </application>

</manifest>